# Configuration

Portable examples and schemas live here. `demo_lab.yaml` uses schema version 2
to describe the Phase 1A.1 Station → StationSlot → Sample hierarchy. Slot pose
references are semantic identifiers only; the file contains no real coordinates
or hardware connection settings.

Machine-local addresses, ports, calibration, teach points, secrets, and runtime
state must not be committed.
